# AGENTS.md — 项目协作规则

> 本文件是给 AI 助手 / 自动化会话的长期规则，每次会话开始前先阅读；与代码冲突时以代码为准。

## 任务收尾规则（强制）

每完成一个开发任务（功能 / 修复 / 重构 / 测试调整），收尾时必须依次做完以下三件事：

1. **登记改动**：在 `docs/项目记录.md` 追加一节（日期 + 内容摘要 + 冒烟基线变化），
   同步更新受影响的口径文档（`todo.md` / `README.md` / `docs/当前项目设计总览.md` 等，按需）。
2. **梳理后续优化点**：把本次发现但未做的问题 / 可打磨点写入 `docs/体验优化清单.md`
   （按 P0 正确性 → P1 高价值 → P2 打磨分级，附代码位置与建议改法）；
   已完成的条目更新状态标注，不要删除——清单同时是「做过什么」的档案。
3. **回归 + 提交**：跑 `node tools/smoke.js` 全绿后一并 commit（提交信息概括改动）；
   推送远端需走代理：`git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push`。

## 其它约定

- **冒烟测试是回归基线**：改 UI 输入流程（choose / journal / say 等）时必须同步检查
  `tools/smoke.js` 的按键拦截器（`choosePick` / `pressQueue`）——输入阶段语义变化会错位按键序列。
- **存档向后兼容**：存档结构只增不改；smoke 中改动全局状态用 try/finally 快照还原。
- **行号口径**：文档中的代码行号引用以最近一次仓库状态为准，改代码后同步修正文档。
- **双击即玩**：保持经典 `<script src>`（可 defer）、无 module、无本地 fetch 的 file:// 兼容约束。
