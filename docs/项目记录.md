# 项目记录 — 开发大事记

> 本档案自 2026-09-20 起建立。此前的进度散见于 `todo.md` 与各设计方案文档，第一节按其口径回溯整理；
> 之后每完成一个分期，在此登记「日期 / 内容 / 冒烟基线」。

## 一、里程碑回溯（据 todo.md 与设计文档整理）

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0–P3 | bug 速修 / 系统补全 / A1–A3（矿道·图鉴·作业）/ B1–B5 | ✅ |
| s1–s4 星露谷分期 | s1 特色摸底 / s2 工具热键栏 / s3 五系技能升级（农·矿·渔·战·社交）/ s4 农场扩容+洒水器·肥料·稻草人 | ✅ |
| s5–s8 | 畜牧扩展 / 季节任务链+村民帮助板 / 四季×农场节日 / 好感心级解锁事件 | ⬜ 顺延 |
| 精灵对战（2026-09-19 立项） | T1=s9 精灵小筑 / T2=s10 战斗强化 / T3=s11 宠物对战，方案见 [精灵对战与宠物店设计方案.md](精灵对战与宠物店设计方案.md) | ✅ 2026-09-20 |

- **2026-09-20（上一会话）**：s9–s11 一次性落地——精灵小筑三入口（球·口粮 / 日期种子珍稀架 / 收购台）、
  MOVES 22 招注册表 + 四键菜单 + 武魔防读招反震 + PP 制、草丛遭遇改画布 petMode（三系克制环 /
  GBA 捕捉公式 / 体力跨场携带 / 无伙伴脱身战）。冒烟 928 → **986/0**
  （新增 [5.51] 20 条 / [5.52] 11 条 / [5.53] 21 条，后两段直接重载真实 battle.js）。

## 二、2026-09-20（本会话）：生物课 × 精灵线联动（s12–s13）

> 方案：[生物课与驯兽导师设计方案.md](生物课与驯兽导师设计方案.md)
> ——回应「宠物精灵系统与学校的学习和老师的关联如何增加」；一期/二期完成，
> 三期（吴老师 petMode 支援 / 斗虫大会 / 隐藏导师）留待后续。

### s12 驯兽知识与导师（一期）

- **驯兽题库 `TAMING_QUIZ` 13 题**（5 道克制环专题 chart:true + 8 道驯兽常识），讲堂与讨教共用。
- **蒲老师「克制环讲堂」**（magic 区菜单新增第 4 项，原三项下标不动）：三页相克环讲解 →
  `seededShuffle(chart, day*29+7)` 抽 3 题随堂问（答错可重答）→ 全对出师 `f.tame.chart=true`
  +8 文 + 学院杯 + toast；出师后再选变为温习。
- **阿橘「驯兽讨教」**（精灵小筑菜单新增第 4 项，同保证 append-only）：`TAME_LESSONS` 三招递进——
  轻声细语 `soft`（捕捉率 +10%）→ 察言观色 `eye`（道具栏选中球显示估算捕捉率）→ 战后急救 `aid`
  （出画后伙伴自行恢复三成）；学费 60/100/160 文，**答错退学费**（焦点在知识，不罚钱）。
- **battle.js 挂接**：`catchChance` 增第 5 参 `tame`（进乘区、同受 92% 封顶）；渲染挂接——
  菜单页属性优劣常显行（它怕X·克Y / 我方克制位）、技能列表每招 `↑/↓/－` 克制标记、
  球悬停「捕捉率约 NN%」（需 eye）、菜单页察言观色提示。

### s13 知识树反哺与学业奖励（二期）

- **`petBonus(nodes)` 纯函数**（battle.js 导出）：英语→速度（封顶 +3）/ 数学→会心率（5% 底数，
  每节点 +2%，15% 封顶，会心 ×1.5）/ 语文→鼓劲 ×(1.35 + 0.03n) / 科学→守护减伤（0.5 下探 0.35）。
  petMode 入场按 `Mistake.unlocked` 挂 `act.edu`，入场日志播报「📖 课堂知识在支持着…」。
- **`petExamGift(tier)` 学业奖励汇入精灵线**：周测满分 → 阿橘赞助高级球；月考满分 → 蒲老师口粮 ×2；
  期中 S 级 → 未收录 rar≤2 按当日种子赠**金色异色**小伙伴（图鉴全收录折现 +100 金兜底）。
  分别挂在周测 / 月考 / 期中的满分分支，`S._petGift` 为测试钩子。
- **成就三项**：生物课代表（讲堂出师）/ 驯兽学徒（三招学齐）/ 知识驯兽师（知识树 15 节点且图鉴 10 种）。
- **存档兼容**：只增不改——新增 `f.tame`（驯兽技巧位）与 `f.kp` 的战斗侧消费，旧档自动可用。

### 测试与口径

- smoke 新增 **[5.54] 8 条 / [5.55] 11 条**（同样重载真实 battle.js、`finally` 全量还原）：
  捕捉 tame 参单调与封顶 / 讲堂·讨教真实场景流（choosePick 按正确项文本作答，选项洗牌免疫）/
  战后急救 8→27 / petBonus 四维与封顶 / 知识树实战（速度 +3、会心 32→48、入场日志）/
  三档考试礼物 / 成就三项。
- 回归：**1005 通过 / 0 失败**（986 → +19）。
- 同步口径：`todo.md`、`README.md`、`docs/当前项目设计总览.md`（本文件即本次新增）。

## 三、2026-09-20（本会话·续）：剧情与游戏性一期（s14）

> 方案：[剧情与游戏性改进方案.md](剧情与游戏性改进方案.md)
> ——回应「章节/情节/游戏性不足」品评；P-A 期四项（A1/A2/A3/A4）本次全部落地，
> P-B 期（四季花园祭坛、NPC 微剧情、武馆×宠物交叉、NG+ 差异化、强度墙）留待后续。

### A1 影子线缝合（theme → cause）

- **t5Mirror**：镜前战胜后，灰先生补一段托付——遗忘之雾梦话自述「来自一个谁也不记得的地方」，
  请你「把名字还给它」（第三幕 BOSS 的回收钩子）。
- **bossMirror**：贪影现身时自白与遗忘之雾同源（「从我身上逃走的一角」），索求从宝石升级为
  「更重的东西」——反派动机与主线道具产生因果。

### A4 三章中点过场（章内小高潮）

- 新增 `chapterMid(n)`（一次性旗标 midCh1/2/3）：ch1 徽章≥2 团子落肩 + 石门心跳；
  ch2 两门魔法课后星儿雾中低语「影子在长夜里变浓了」；ch3 第二段时代记忆点亮后
  镜中人影 + 灰先生托付回响。挂点：教师挑战 gainBadge 后 / `checkClasses`（改 async，4 调用点 await）/ eraDone 末尾。

### A2 亲密度对战化（buddyBond 进战斗）

- battle.js petMode 入场计算 `edu.bond`（≥30 → 1 / ≥60 → 2）：**bond 1** 回合末双方站立回 1 体力；
  **bond 2** 首次被击倒稳住剩 1HP（整场一次，`bondUsed` 置位）。入场日志播报亲密度档位。
- 漫步/摸头/喂面包攒的亲密度从纯展示变为对战资源（喂面包等日常行为获得战斗收益）。

### A3 魔法课机关化（谜题形式收敛）

- **魔药课**：单选配方 → 「月露调配」两步机关（主料→药引）；**咒语课**：单选读音 →
  「三烛辉映」三步机关（铜牌口诀 金→蓝→银）。新增 `MECHANISMS` 5 条导出（与 RIDDLES 同形，
  正确项刻意放非 0 位）；RIDDLES 由 5 题收敛为 3 题（回廊 t4/t5 下标同步重排）。

### 测试与口径

- smoke 新增 **[5.56] 7 条**（重载真实 battle.js、`finally` 还原 buddyBond）：bond 1 回气数值链
  （35 伤 → -2 → +1 = 19）/ bond 2 击倒稳住（2 → 0 → 1，bondUsed 置位）；拦截器池接入 MECHANISMS。
- 顺手消抖：[5.51] 新种购入 `buyCritter('i4')` 的 5% 异色 roll 补定桩（既有偶发失败源）。
- 回归：**1012 通过 / 0 失败**（1005 → +7，三连跑全绿）。
- 同步口径：`todo.md`、`README.md`、`docs/当前项目设计总览.md`。

## 四、2026-09-20（本会话·再续）：周末系统打磨（s15）

> 方案：[打磨与成就扩展方案.md](打磨与成就扩展方案.md)
> ——兑现 todo「二、已知小缺口」三项 + 「四、P4」成就墙联动，全部为确定性打磨。

- **C1 电影夜票根收藏品**：photos 类新增 `p_ticket`（回忆照片 15→16 条），首次电影夜
  `gain('photos','p_ticket')` 入册（gain 自带去重，重复观影不重复发）。
- **C2 电影夜片单动态化**：剧情同伴（小影/墨墨/金鹏/陆飞/林小雨/月见）友谊二阶（stage≥2）入片单，
  六人组保底；day 取模选友公式不变。
- **C3 跳蚤集市扩容**：`FLEA_EXTRA`——石门开启 → 铜/铁矿，期中过后 → 金矿，上 2/3 号摊；
  **头牌位保持家常货池**（兼容旧档选品记忆与 smoke 既有 day7=coffee 断言）。
- **C4 成就墙四项**：电影发烧友（movieNights≥5）/ 集市常客（新增 `fleaVisits` 计数器）/
  捕虫高手（新增 `catchTotal`，池塘/挥网/笼捕/球收四点累计）/ 珍稀架收藏家（复用 legendTrio）。
- **测试**：smoke 新增 [5.57] 10 条（NG+ 后 flags 自行定桩 + try/finally 快照还原）；「测试侧遗留」
  小猫 flake 项经多轮全绿关闭。回归：**1022 通过 / 0 失败**（1012 → +10，三连跑全绿）。
- 调试插曲：片单断言首版用 day28——`28%12=4` 落在合并名单 base 段（大壮）而非扩容段，
  改 day105（`105%12=9` → 白名单第 3 位陆飞）后可区分硬编码/动态两种实现。

## 五、2026-09-20（本会话·终）：体验收束 + P-B 期 + 驯兽三期 + 星露谷 s5–s8（s16）

> 一次性落地「体验收束」两条主线、剧情与游戏性 P-B 期五项、驯兽三期三项、
> 周日钓鱼大赛与星露谷分期 s5–s8。冒烟 1022 → **1081/0**（新增 [5.58]–[5.68] 十一段 59 条，三连跑全绿）。

### 体验收束（一/二）：信息与奖励语言收敛

- **系统开张导览**：`SYSTEM_TOUR` 八档（greet→gift→mini→fish→pet→farm→dojou→bug）按进度逐个
  「开门」，`systemTour()` 每天清晨最多广播一条开张提示（`f.tour` 登记）；任务清单在新生阶段
  （arrival/settle）不再列出图鉴进度轰炸。
- **结算单收获行**：`f.daySnap` 快照图鉴/知识点当日基准，睡前结算单新增
  「🌟 今日收获：图鉴 +N · 知识点 +N」，把全天散落的奖励语言收束成一句。

### P-B 期五项（剧情与游戏性二期）

- **P-B1 晨曦祭坛（情5）**：四季花园正中新增 altar 物件 + 交互点 `S.dawnAltar`——四灵不全时沉睡，
  四灵苏醒后晨曦女士现身、答对「圣物之问」（花园即圣物）→ `f.dawnAltar` 点亮祭坛，
  回忆照片 `p_dawn` 入册 + 精力上限 +10 + 成就「晨曦的祝福」。
- **P-B2 微剧情二号线（情4）**：三条三段式支线——陆飞·幸运腕带（校园看台新交互点
  `grandstandStrap` 寻回）、林小雨·淋湿的《绿山墙的安妮》（秦墨授修书秘方）、
  小刚·给爸爸的纪录（器材室借秒表计时破纪录）；各赠回忆照片 `p_run`/`p_rain`/`p_record` + 好感 10。
- **P-B3 武馆×宠物战交叉（玩2）**：援护技——带随行伙伴（体力过三成）打训练师战，
  首次被打到濒危线（≤40%）时伙伴扑出咬一口（`assistPower = max(3, round(力×2.5))`）+ 续命 6 点，每场一次。
- **P-B4 NG+ 差异化（玩6）**：`NG_REUNION` 四人（校长/小明/小红/乌云）重逢过场（`f.ngMeet` 一次性）；
  `npcMindLine` 新增随身宠物话题池与 NG+ 图鉴完成度话题池；NG+ 序章专属文本。
- **P-B5 强度墙（玩4）**：挑战榜陪练菜单新增「极限连战」（扫地僧认可/毕业后开放）——
  六关 tier 递增（battle.js `opts.tier`：血 ×(1+.35(t-1))、攻 ×(1+.18(t-1))、金币 ×(1+.25(t-1))），
  `f.rushBest` 纪录 + 成就「连战入门」「不败之壁」。

### 驯兽三期（生物课 × 精灵线）

- **三期A 吴老师支援特训**：咒语教室新菜单项（100 文，学过任一咒语）→ `f.tame.wu`；
  petMode 菜单变四项（招式/道具/**支援**/逃跑）——星之盾（守护）/晨光治愈（回 30%）/
  缚影咒（敌方乏力）/月光弹（魔法一击），每场各一次。
- **三期B 校园斗虫大会**：三系擂台逢 9 的日子（第 9/19/29… 天）开赛，虫系专属三连胜
  （日种子对手、每场满血）→ `f.bugKing` + 金币 50 + 学院分 10 + 成就。
- **三期C 后山隐藏导师**：后山东北角石屋 `S.hermitHut`——克制环出师 + 图鉴 20 种 +
  随行伙伴 + 200 文 + 相克题 → 「进化催化」`f.buddyBoost`（力/速 +1，battle.js 与 wildBattle 自动消费）。

### 周末补全 + 星露谷 s5–s8

- **周日钓鱼大赛**：河畔 🏆 报到处（day%7===0 且白天）——三竿按鱼种稀有度折「磅数」，
  `contestRank` 60/40 分档冠军/亚军；`f.contestBest` 纪录 + 成就「钓鱼大赛冠军」。
- **s5 畜牧**：田伯处买小牛犊（120 文）/小羊羔（100 文）→ 后院牛棚/羊圈（sprites 新增
  `cowShed`/`sheepPen` 两款像素小屋）；牛日产奶一次（喂熟双份）、羊三日剪毛一次，
  `farmTick` 结算，物品新增 milk/wool/calf/lamb，挤奶剪毛走 farm 技能经验。
- **s6 帮助板**：小镇 📋 板 `S.helpBoard`——11 条委托池（含季节限定与畜牧联动）按日种子刷 3 条，
  交货得酬金 + `f.helpPts` 声望（3/7/12 里程碑）+ 成就「小镇之光」。
- **s7 节日联动**：`FESTIVAL_LINKS` 七节（植树/读书/风筝/冬日祭/踏青/纳凉/收获）在校务板
  打卡后开当日小活动（`f.fest[day]` 登记），日期与 cal.FESTIVALS 表对齐断言。
- **s8 心级事件**：好感 ≥90 时六位挚友（小红/小明/乌云/大壮/小影/陆飞）各一段心里话过场
  （对话包装器 `maybeHeartEvent` 挂接），六段集齐 → `p_hearts` 全员大合影 + 成就「心满益善」。

### 测试与口径

- smoke 新增 **[5.58]–[5.68] 十一段 59 条**：导览节拍与手册减量 / 祭坛唤醒链 / 三条微剧情全链 /
  NG+ 重逢一次性 / 连战纪录 + tier 缩放（重载真实 battle.js 断言 act.e 血攻等比）/
  支援指令状态机 + 催化加成进战斗（重载真实 battle.js）/ 大赛分档纯函数 + 参赛链 /
  畜牧两条生产线 / 帮助板委托链 / 节日表对齐 / 心级事件六连。
- 「生物图鉴」存储键口径修正确认：`critters` 经 `CAT_MAP` 读写 `col().insects` 槽（测试已按此定桩）。
- 成就墙新增 10 项：晨曦的祝福 / 心满益善 / 不败之壁 / 连战入门 / 斗虫大会冠军 /
  钓鱼大赛冠军 / 牧场主 / 小镇之光 / 驯兽宗师（另 s13 遗留口径并入）。
- 回归：**1081 通过 / 0 失败**（1022 → +59，三连跑全绿）。
- 同步口径：`todo.md`、`README.md`、`docs/当前项目设计总览.md`、本文件。

## 六、2026-09-21：实现层体验修复（P0 ×5 + P1 ×11）

> 依据：[体验优化清单.md](体验优化清单.md)（同日全量代码核查产出）。改动 +938/−170，commit `1a1227f`，
> 涉及 main/ui/engine/audio/game/ai 六模块 + index.html / style.css。回归：**1081 通过 / 0 失败**（断言数不变）。

### P0 正确性（5/5）

- **① 手册技能页键盘不可达**：main.js 手册分支接入现成 `ADV.UI.journalMove`（↑↓ 切组 / ←→ 切页 / PgUp·PgDn）。
- **② choose 说明文字超 3 行截断**：caption 分页化（`capPages` 每页 3 行，Z 翻页 / X 直达选项），
  选项阶段独立接输入；渲染带页码与 ▼ 闪烁——剧情台词不再丢句。
- **③ 长列表溢出**：choose 加 7 行可见窗口 + 滚动（`chooseGeom`/`clampChooseScroll`，▲▼ 指示）；
  好友页 clip 渲染 + `friendScroll` 滚动（滚轮 / ▲▼ / 行号指示）。
- **④ 主循环无保护 + 坏档必崩**：loop 包 try/catch(`showFatal` 单例错误界面) + finally 续 rAF；
  `unhandledrejection` 接温和 toast；`load()` 语义校验（`validSave`）；非法 map 回退 `homeIn(18,11)`。
- **⑤ 存储写失败静默**：save() catch → 一次性警告 toast；`storageOk()` 启动探测 + 标题页警示；
  `hasSave()` memo 化（顺带修掉 P2-20 标题页每帧 JSON.parse）。

### P1 高价值提升（11/11）

- **⑥ 启动黑屏**：纯 CSS splash（标题渐显 + 加载点动画）首帧后移除；16 个脚本全部 `defer`。
- **⑦ 存档导出/导入**：`exportSave()`（base64，剪贴板优先 prompt 兜底）/ `importSave()`（校验后直接读档进游戏）；标题页入口。
- **⑧ AI 出题等待**：出新题前 toast「🤖 老师正在现场构思新题……」；单模型超时 20s → 8s。
- **⑨ 对话回看**：`msgHistory`（60 条）+ L 键回看层（↑↓ 滚动；打开期间屏蔽世界输入与对话推进）。
- **⑩ 触屏点击**：canvas pointerdown 反算逻辑坐标 → `tapAt`（选项行 / ▲▼ 滚动 / 窗外取消）+
  `journalHit`（页签/分组/翻页/关闭）+ 标题菜单行点击 + 对话框点按翻页。
- **⑪ 全屏**：工具条 ⛶ → `toggleFullscreen`（webkit 前缀兼容，切换后重 `fit()`）。
- **⑫ 贴墙滑动**：updatePlayer 对 held 方向逐个尝试，全部失败才停——斜按沿墙不再卡死。
- **⑬ 音频三连**：playBgm 走 musicBus 交叉淡切（旧 0.3s 出 / 新 0.9s 入）；`volMode` 持久化；
  触屏 pointerdown 先 `Audio.init()` 再 resume（修 iOS 首点丢声）。
- **⑭ Toast**：时长按字数自适应（上限 5s）、队列 3 → 5、独立 `renderToasts` 并在 HUD 展开时避让。
- **⑮ 商店**：列表显示「已有 ×N」；购买后子菜单「买 1 / 买上全部（金币约束）/ 返回」。
- **⑯ 对话中开手册**：打开前置 `UI.busy` 守卫；交互期间 update 喂空 held——幕后不再推进对话。

### 测试与口径

- smoke 断言数不变（1081），三处适配：拦截器 plan 在有 caption 时前置 `{cancel:true}` 直达选项
  （分页阶段会吞 down/ok，否则答题按错陷入重问死循环——**重要经验**，已写入 AGENTS.md）；
  toast 队列测试按新上限 5 改写并改调 `renderToasts`；giftShop picks50 序列适配「买 1」子菜单。
- 新增根目录 `AGENTS.md`：**任务收尾规则**（登记改动 + 梳理后续优化点 + smoke 回归后提交推送）自此固化。
- 同步口径：`todo.md`、`README.md`、`docs/体验优化清单.md`（P0/P1 全部标记落地，P2 为后续方向）、本文件。

## 七、2026-09-21（本会话·续）：P2 打磨批次（17–19、21–24 全落地）

> 依据：[体验优化清单.md](体验优化清单.md) P2 条目，按 AGENTS.md 收尾规则推进。涉及 engine/ui/main/ai 四模块 +
> style.css / index.html。回归：**1081 通过 / 0 失败**（断言数不变，P0/P1 批次的拦截器适配全部沿用）。

### 性能（17 / 18 / 19）

- **17 夜景层缓存**：`drawNightStreet` 按 `map.id + '_' + period` 签名缓存整层离屏 canvas（`nightCv/nightSig`，
  loadMap 失效重建），门灯渐变按 `x|y|r` 键入模块级 `glowGrads` Map——夜晚每帧的全图网格扫描与逐门建渐变归零。
- **18 视口裁剪 + 闭包削减**：render 改扁平 `{y, …}` 列表排序后单循环分发（去掉每帧 100+ draw 闭包），
  `drawObj/drawChar` 提为模块函数；物件按相机范围（x ±192 / y ±160px，容纳最宽物件）裁剪，角色 ±96px，
  sparkle 按屏幕范围跳过。
- **19 天气层缓存**：星空 40 颗坐标预计算进 `STARS`（帧间只改 globalAlpha）；雨雾/寒潮渐变按档位缓存
  （`fogGrads/coldGrads`）；暴雨 180 滴合并单 path 一次 stroke。

### 视觉与信息（21 / 22 / 23）

- **21 小屏清晰度**：画布显示宽小于原生（`L.cw < W-1`）时加 `.soft` 类，CSS 切回 `image-rendering: auto`——
  下采样不再发糊；r<0.75 大字版布局留后续（已入「后续可做」表）。
- **22 对比度/色盲冗余**：空态文字 `#667` → `#9aa0c0`（对话日志/日历/收藏 3 处）；成就未解锁 alpha `.4` → `.65`；
  迷你 HUD 四宝石加学科首字（数/语/科/英）；小地图 NPC 方点 → 圆点（形状+颜色双编码）。
- **23 列表滚动**：任务/背包页接 `listScroll`（可见 14/12 行 + PgUp·PgDn/▲▼ + 「N-M/共 X 项」指示）；
  收藏页头补「收录 X 种，列前 10」说明；`journalPage(dir, tab)` 按页签路由到好友/通用滚动变量。

### 细节八件（24）

- 虚拟按键默认显隐：`padPref`（null=跟随设备：触屏显示/桌面隐藏只留工具条）+ `body.nopad`，V 切换并记住；
- `pagehide` 兜底存档（play 态且非 busy/手册）；AI 降级一次性 toast（未开启/额度用完/连不上三路）；
- viewport 移除 `user-scalable=no`（低视力玩家可辅助缩放）；过图存档挪 `setTimeout(0)` 错峰；
- HUD 三态 full→mini→hidden 循环（`campus_hud` 持久化 + 旧 `campus_hud_mini` 迁移，hidden 时跳过绘制）；
- pose 表缓存键 `_poseKey` 替代每帧 `JSON.stringify(P)`。
- **⑧ findTarget 帧内缓存：实测后回滚**——smoke 5.17 在 time 不变的同一帧内改写 NPC 格位再查，缓存返回陈旧
  null。根因：缓存键（玩家格+朝向+time）枚举不全，NPC/物件位置是隐藏输入。教训已写入清单落地记录：
  **缓存键覆盖不了全部输入时宁可不缓存；smoke 直接操纵内部状态的写法恰好能暴露这类问题。**

## 八、2026-09-21（本会话·再续）：玩法批次一（C6 成长报告 + C4 办公时间 + C2 借书 + P1 成就闭环）

> 依据：[玩法灵感清单.md](玩法灵感清单.md) 优先级建议第 1、2 条，按 AGENTS.md 收尾规则推进。
> 涉及 game/mistake/ui 三模块 + smoke。回归：**1084 通过 / 0 失败**（基线 1081 + [5.69] 新增 3 断言）。

### C6 期末成长报告

- 新增 `growthReport()`（game.js）：聚合全学期 `quizLog` 答题数/正确率、知识点总量、图鉴与累计收服、
  好友进度/帮助声望/连续打卡、学院分/零花钱 + `Growth.aggregate()` 五维之最，
  按五维最高项给定制评语（学识→王老师 / 体魄→刘老师 / 心性→校长 / 人缘→同学们 / 技艺→美术老师）。
- 毕业仪式接入：结局台词后播报「📋 期末成长报告」+「✉️ 老师评语」两页（在 200 文毕业礼金**之前**结算，数字真实），
  并记大事记「领取了学期成长报告」；手册成长页底部新增同源常驻报告条（ui.js renderGrowth，
  每行 `slice(0,26)` 截断防压右侧大事记列，try/catch 保护 smoke 环境）。

### C4 老师办公时间（午休 = period 2）

- `trial()` 开头检测 `ADV.Cal.period === 2`；作业分支：办公时段全对奖励翻倍（金币 8→16、学院分 1→2）
  + 开场/获奖专属台词（`hwOffice`/`hwGreatOffice` 可覆写，缺省文本已配）。
- 正式试炼分支：办公时间开场白 + 「首错当面重讲」——首次答错后错题数清零一次（`forgiven` 旗标），
  实现为「答错不记账」的答疑豁免；其余时段行为完全不变。

### C2 图书馆借书机制

- 新增 `BOOK_SUBJECT`（4 本课本→科目映射）与 `libMenu()`：秦墨处借书（`f.borrow={id,due}`，借期 3 天），
  借阅中/逾期/未借三态菜单。
- **借书 buff**：`takeClass()` 结算点（kp 增量 >0 且课本科目==当日主科且未逾期）知识点额外 +50%
  （`Mistake.gain(en, ceil(gained/2))`），toast 提示「《xx》派上用场」。
- 按时归还：奖 6 文 + 对应科目知识点 +2；**逾期偏差**：扣 0 声望改罚金 `min(gold,10)` 文并强制收书
  （避免损伤「小镇之光」成就进度），记大事记。
- `Mistake.gain` 导出（原为模块内部函数）；S.librarian 任务后三个出口（libDone / n>=3 / 未满 3 本）
  均接入常设借阅台，借阅列表 = `Books.byWhere('library')`（4 课本 + 5 经典共 9 本，经典书无科目 buff）。

### P1 闪光个体成就闭环 + P2 核对

- 成就表新增「金色传说」：`Collect.shinyCount() >= 3`（异色机制 5%×shinyMult 本已存在，补收集终点）。
- P2 天气/时段限定遭遇**核对确认已存在**（critter `when:{p,se,we}` + whenOk 过滤 + 图鉴线索），无需改动。

### smoke [5.69]

- 三断言：growthReport 四行+评语结构 / `Mistake.gain` 导出 / 成就表含「金色传说」；
  纯数据断言，无输入驱动，不触碰按键拦截器。

## 九、2026-09-21（本会话·三续）：玩法批次二（C1 学科奥赛 + S1 工具锻造 + S3 夜钓限定）

> 依据：[玩法灵感清单.md](玩法灵感清单.md) 优先级建议第 3、4 条，按 AGENTS.md 收尾规则推进。
> 涉及 game/collect/maps/minigame 四模块 + smoke。回归：**1092 通过 / 0 失败**（基线 1084 + [5.70] 新增 8 断言）。

### C1 学科奥赛线（教室 🏆 公告栏）

- **入口**：教室新增 `{ kind:'sign', s:'olympBoard' }` 公告栏对象（maps.js，坐标 25,2）——刻意不挂进
  教室黑板菜单：smoke 对 `S.classBooks` 有 4 次 pump 序列，菜单加项会错位按键；独立对象零干扰。
- **赛制**（game.js `OLYMP` 配置 + `olympState()`）：每季一科——春数学（王老师）/ 夏科学（陈老师）/
  秋语文（李老师）/ 冬英语（吴老师）；季节日 `sd≤3` 报名、`sd4-7` 备战、`sd===8` 比赛日，`sd>8` 缺席作废。
- **备战度**：`ask()` 尾部挂钩子 `olympPrepTick(subject, correct)`——备战期内讨教/作业/试炼答对且科目匹配
  当季奥赛科 → `st.prep++`（每满 3 次 toast 鼓励）；随堂 examAsk 不计入（见体验清单遗留项）。
- **限时赛** `olympContest()`：按 `C.day*17+科目长度` 种子抽 5 题（seededShuffle，NG+ 不重排），
  备战度 ≥3 给「首错豁免」；错 0 冠军 / ≤1 亚军 / ≤2 季军 / 其余优秀奖。
  冠军：+60 文 +6 知识点 + 📸「奥赛领奖台」照片 + 成就「奥赛金牌」；亚/季军 +30/+15 文。
  集满 3 个不同科目冠军 → 成就「全能学霸」。
- **状态存档**：`f.olymp[season] = { reg, prep, rank }`（只增不改，旧档自动 `olymp={}` 初始化）。

### S1 工具锻造（老矿工锻炉）

- `FORGES` 三件套（game.js）：**宽口锄**（铜矿×3 + 30 文，翻土十字顺带翻邻格）、
  **三嘴壶**（铁矿×3 + 50 文，浇水顺带浇左右田垄）、**硬调竿**（金矿×2 + 宝石×1 + 80 文，搏鱼窗口 92→110px）。
- `forgeMenu()`：挂在老矿工剧情走完后的常设出口——仅当「身上有 4 类矿石之一或已有锻造件」才出现，
  smoke 的单次矿工 pump 停在初见剧情分支，不受影响。
- `S.farmPlot` 翻土/浇水分支各加一级工具加成（邻格只对空地翻土/对已种植未浇水地块浇水，
  额外精力走 `C.costEnergy`），`f.toolLv[key]=1` 存档只增不改。

### S3 夜钓 + 时段限定渔获

- `rollFish` 重写补夜钓层：夜晚追加 **月光鱼 f9**（权重 ×(1+鱼饵加成)）；雨夜再追加传说
  **霓虹鲤 f10**（+80 文 + 大事记「在雨夜钓到了传说中的霓虹鲤」）；白天渔获表完全不变。
- 两条鱼入 collect.js 图鉴（f9 仅图鉴、f10 高价传说）；`FISH_SCORE` 补 34/70（钓鱼大赛固定白天
  period 1-3，限定鱼天然不进赛池，分数仅保险口径）。
- 成就 +2：「夜钓客」（钓到月光鱼）、「雨夜传说」（钓到霓虹鲤）；夜钓时河边标题栏加「· 夜钓正当时」提示。
- minigame.js：鱼窗宽度支持 `opt.wide`（硬调竿 110px / 普通 92px / 简单 112px）。

### smoke [5.70]

- 8 断言：OLYMP 四季配置齐全（科名/老师/科目键）/ 教室含 olympBoard 对象 / FORGES 三件套
  （矿耗+金价字段）/ 图鉴含 f9（desc 含「夜」）与 f10（desc 含「雨夜」）/ 成就表含
  夜钓客·雨夜传说·奥赛金牌·全能学霸。纯数据断言，无输入驱动。

## 十、2026-09-22（本会话·四续）：玩法批次三（P4 图鉴交换所 + S5 料理 buff）

> 依据：[玩法灵感清单.md](玩法灵感清单.md) 优先级第 5 条，至此清单 P0-P5 主体全部落地。
> 涉及 collect/cal/mistake/engine/game/ui 六模块 + smoke。回归：**1108 通过 / 0 失败**（基线 1092 + [5.71] 新增 16 断言）。

### P4 图鉴交换所（精灵小筑阿橘柜台）

- **入口**：`S.petShop` 主菜单新增「图鉴交换」（插在「驯兽讨教」与「先逛逛」之间，0-3 序号不变；
  smoke 的 petShop pump 全部以 `indexOf('先逛逛')` 串匹配找出口，序列零干扰）。`S.exchange` 独立场景。
- **定向交换**：先挑目标（未收录 species 按稀有度排序，换资 💰30/60/100 按目标 `rar`）→ 再挑换出伙伴
  → 确认。`Collect.exchangeCritter(giveId, getId)`：守卫（图鉴存在 / 在手 / 非异色 / 非随行 /
  目标未收录 / 金币足）→ 换出个体 `gone=1`（**图鉴收录保留**，可从珍稀架再接回——与收购台口径一致），
  新个体 `rollShiny(1)` 异色判定；`checkCritterMiles`/`checkFull` 照常结算。
- **每日免费交换请求**：`dailyOffer()` 以日期为纯种子（LCG，`day*2246822519`）确定性生成
  `{give, want}`——give 取手上非异色非随行一只、want 取未收录且稀有度不降的一只；同一天内不变。
  `doDailyOffer()` 执行一换一并**退回换资**（文案承诺免费——smoke 联调中发现 exchangeCritter 会扣费，
  已修复为如数退回）；手上无可换伙伴时作废，明日刷新。
- **导出**：collect.js 新增 `exchangeCritter / dailyOffer / doDailyOffer / EX_COST`。

### S5 料理 buff 化（三种当日临时加成）

- **状态**：`f.buff = { mem|vig|swift: day }` 只记日期，`buffActive(k)` 按 `=== C.day` 判定——
  过夜自然失效、无需清理代码；同类不叠加（后吃覆盖）；存档只增不改。
- **三钩点**（各一处、全局入口）：
  - 🧠 好记性（r4 月光羹 / r7 田园时蔬汤 / r9 炭火烤鱼）：`mistake.js answer()` 答对分支额外
    `gain(subject, 1)`——随堂/作业/讨教/考试/打卡/奥赛备战题全走此入口，天然全覆盖；
  - 🍖 精力充沛（r1 蛋包饭 / r2 排骨饭 / r5 矿工炖菜）：吃下立即 `gainEnergy(12)`，且 `cal.js
    costEnergy()` 当日消耗打八折（`Math.round(n*.8)`，保底 1）；
  - 👟 健步如飞（r3 星星曲奇 / r6 四季春卷 / r8 南瓜派）：`engine.js updatePlayer` 步频
    `.22→.18`（冲刺 `.13` 不变）。
- **入口**：`S.dinnerTable` 菜单在「自己做顿饭」之后插入「吃点自己做的小菜」（背包有 r1-r9 才出现，
  既有字符串拦截器不受扰）；`eatAtHome()` 选菜→消耗→上 buff→toast 说明。
- **食谱区**：`ui.js renderBag` 底部固定区块 `renderBagRecipes`——已学食谱 + buff 图标（当日生效的
  高亮 ✓），置底不与物品列表滚动冲突，空背包也显示。
- 九道菜 desc 全部追加吃法提示（背包页可见）；game.js 导出 `DISH_BUFF / BUFF_META / buffActive`。

### smoke [5.71]（16 断言）

- S5 数据面（九菜映射 / 三 buff 元信息 / desc 提示 / 饭桌菜单序）+ 功能面（`Mistake.answer`
  知识点 1→2、`costEnergy` 10→8、buffActive 判定）；
- P4 面（petShop 菜单源串序 / API 导出 / 三道交换守卫 / dailyOffer 确定性 / 定向交换扣费与收录保留 /
  doDailyOffer 免费一换一 + 无伙伴作废）；
- 全块 try/finally 快照还原（col/gold/buddy/buff/quizLog/wrong/kp + `Cal.dump()` 整体回填），
  `Math.random` 块内定桩 .99 消除异色 5% 抖动（dailyOffer 走自身 LCG 不受影响）。

## 十一、2026-09-22（本会话·五续）：体验优化批次（锻造 II 级 + 随堂备战度 + 已成交标记 + HUD buff/图鉴徽记）

> 依据：[体验优化清单.md](体验优化清单.md)「后续可做」表 5 条遗留 + 防御性保留 1 条，另补设计总览 2.10 时间两轴口径。
> 涉及 game/collect/minigame/ui 四模块 + smoke。回归：**1116 通过 / 0 失败**（基线 1108 + [5.72] 新增 8 断言）。

### S1 锻造 II 级（开垄锄 / 五嘴壶 / 龙骨大物竿）

- **数据**：`FORGES` 三件套各加 `next: { name, ores, gold, desc }`——开垄锄（铁矿×4+铜矿×2，工钱 120）、
  五嘴壶（金矿×1+铁矿×3，150）、龙骨大物竿（宝石×2+金矿×3，200）；`f.toolLv` 由 0/1 扩为 0/1/2（存档口径不变，只加值域）。
- **forgeMenu 分级**：lv 0 出 I 级、lv 1 出 II 级（`tool II → next.name`），lv 2 不再锻造；成功写 `f.toolLv[key] = x.tier`。
- **效果接线**（id 列表换法，I 级行为原样保留）：
  - 翻土：hoeLv≥2 十字 → **整垄**（`pid - col + [0..3]`，跳过自身）；
  - 浇水：canLv≥2 ±1 → **整垄**；均带 toast 提示等级；
  - 钓竿：`wide: (f.toolLv.rod)||0` 传 minigame，鱼窗 92→110→128 三档。

### C1 随堂练习攒奥赛备战度

- `olympPrepTick(subject, correct)` 抽为具名函数并导出到 Game；`examAsk` 整卷通过分支挂同一钩子——
  备战期随堂满分也能攒备战度（与办公时间奖励天然错开：整卷通过 vs 单次讨教）；`S._examAsk` 测试钩子（同 `S._petGift` 惯例）。

### P4 每日请求「已成交」标记

- 成交后记 `f.dailyDone = { day, want }`（只增不改，按日期比较过夜自然失效）；
  `doDailyOffer()` 同日重入直接拒绝（「今天的免费请求已经成交啦」）；`S.exchange` 菜单目标行标「已成交 ✓」。
- 顺带修 smoke 发现的问题：[5.71] 的 finally 补 `dailyDone` 快照还原，杜绝跨块状态泄漏。

### 展示层两件（ui.js）

- **HUD buff 常驻指示**：`activeBuffIcons()` 拼当日生效 buff 图标（🍖/👟/🧠，try/catch 防御），
  迷你 HUD 天气旁与完整 HUD 日历行均接入——buff 存续期间一眼可见；
- **图鉴限定鱼徽记**：`LIMIT_BADGE`（f9 月影鱼→🌙 / f10 霓虹鲤→🌧）挂鱼名后，未解锁 ？？？ 态也可见，兼作出提示。

### 设计总览 2.10（口径文档）

- 补「时间两轴」口径：`PERIODS` 六时段轴（isNight = period≥4，含傍晚）与 `checkedIn` 打卡布尔轴正交——
  打卡不耗时段、只有睡觉跨天；今后深夜场景一律新增 `isDeepNight()`，**不许改 `isNight()`**（详见解 2.10）。

### smoke [5.72]（8 断言）

- 锻造 II 级三断言（FORGES.next 数据面 / farmPlot hoeLv·canLv 源串 / fish wide 传值）；
- 备战度两断言（olympPrepTick 功能驱动：备战期答对 +1、答错与非当季不计数 / `S._examAsk` 钩子源串）；
- 已成交三断言（成交写 dailyDone / 同日拒绝 / 菜单「已成交 ✓」源串）；
- P4 块 try/finally 全量快照（col/gold/buddy/dailyDone/buff + Cal 回填），`Math.random` 定桩 .99 消异色抖动。

## 十二、文档清单

| 文档 | 内容 |
|---|---|
| [当前项目设计总览.md](当前项目设计总览.md) | 系统现状总览（口径随实现滚动更新） |
| [精灵对战与宠物店设计方案.md](精灵对战与宠物店设计方案.md) | s9–s11 方案（已全部落地） |
| [生物课与驯兽导师设计方案.md](生物课与驯兽导师设计方案.md) | s12–s13 方案（一期/二期已落地，三期未做） |
| [剧情与游戏性改进方案.md](剧情与游戏性改进方案.md) | 情节/游戏性品评复查 + s14 改进方案（P-A 已落地，P-B 未做） |
| [打磨与成就扩展方案.md](打磨与成就扩展方案.md) | s15 周末系统打磨方案（已落地） |
| [项目设计优化方案.md](项目设计优化方案.md) | P0–P3 优化方案 |
| [体验优化清单.md](体验优化清单.md) | 实现层体验问题清单（P0–P2 分级；全部条目已落地，附「后续可做」表） |
| [玩法灵感清单.md](玩法灵感清单.md) | 内容侧候选池：宝可梦/星露谷/校园题材未落地借鉴点（含已落地对照表，避免重复建议） |
| ../AGENTS.md | AI 协作规则（任务收尾：登记改动 + 梳理优化点 + 回归提交） |
| ../todo.md | 剩余问题与候选方向 |

## 十三、待办指针

- ~~**git init + 首次提交**~~ ✅ 已完成（2026-09-21）：远端 `kircheisa/campus_game`，推送走代理
  `git -c http.proxy=http://127.0.0.1:7897 push`。
- 浏览器 e2e（s11 遗留）：进店 → 草丛遭遇 → 讨教/讲堂 → 考试领礼真机走查。
- **实机走查（P0/P1/P2 修复后）**：触屏点击命中区、消息回看层、存档导入导出、全屏、
  HUD 三态与虚拟按键默认显隐、小屏 `.soft` 清晰度在真机上的手感验证。
- 候选：见 [体验优化清单.md](体验优化清单.md) 新增「后续可做」表（小屏大字版布局 / 渲染列表按图缓存 /
  星空预渲染 / 存档多槽位 / 对话自动播放 / 触屏 NPC 点选 / 实机帧耗时量化）；
  **内容侧**见 [玩法灵感清单.md](玩法灵感清单.md)（~~三小件+闪光成就~~ ✅ 第八章、
  ~~C1 奥赛 + S1 锻造 + S3 夜钓~~ ✅ 第九章、~~P4 图鉴交换所 + S5 料理 buff~~ ✅ 2026-09-22 已落地，
  见第十章——清单主体至此全部完成；~~锻造 II 级 / 随堂备战度 / 已成交标记 / HUD buff / 图鉴徽记~~ ✅ 第十一章，
  后续可从各条「偏差/余量」列挑点深化：奥赛题库扩充 / 街机厅 S4 / 晨读晚自习 C3 等）；
  以及 game.js 拆分 / 周日活动再扩展 / 料理社交深化 / 经济曲线观察。
